# 工作约定

编写或修改 Markdown 时，同步维护英文 `.md` 和简体中文 `.zh-CN.md` 版本。保留第三方许可证、声明原文及来源信息。

应用范围是基于物体位姿的腕部预抓取，在接触之前停止。不要添加夹爪指令或重复的仿真查看器。导入模块和启动服务时不得连接硬件；自动测试使用显式模拟模式。硬件指令执行必须通过已有的配置、确切计划审核和监督确认关卡。

修改工作流后运行 `python -m pytest -q` 和软件包内的模拟演示。采集数据、本机配置、生成计划、日志和密钥不得提交 Git。不要导入已归档的 `dexpipe` 源码树。
